server {
    listen 80;
    
    location / {
        root /usr/share/nginx/html;
        index index.html index.htm;
        # This fallback is essential for React Router to work properly
        try_files $uri $uri/ /index.html;
    }
}
